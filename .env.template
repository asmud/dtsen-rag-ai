# DTSEN RAG AI - Simplified Environment Configuration Template
# Copy this to .env and modify as needed
# Only essential variables are required - everything else has smart defaults

# ================================
# ESSENTIAL VARIABLES (Required)
# ================================

# Database password (only required variable)
POSTGRES_PASSWORD=rag_pass

# ================================
# OPTIONAL OVERRIDES
# ================================

# Database Configuration (uses smart defaults if not set)
# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_DB=rag_db
# POSTGRES_USER=rag_user
# DATABASE_URL=  # Auto-generated if not set

# LLM Configuration (uses smart defaults if not set)
# OLLAMA_HOST=ollama
# OLLAMA_PORT=11434
# OLLAMA_API=  # Auto-generated if not set
# LLM_MODEL=gemma2:2b
# LLM_FALLBACK_MODEL=llama3

# Embedding Model
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Redis Configuration (uses smart defaults if not set)
# REDIS_HOST=redis
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_URL=  # Auto-generated if not set

# Hardware Settings (auto-detected if not set)
# GPU_ENABLED=  # Auto-detected
# CPU_WORKERS=  # Auto-detected based on CPU cores
# MAX_CONCURRENT_REQUESTS=  # Auto-detected
# EMBEDDING_BATCH_SIZE=  # Auto-optimized for hardware

# Application Settings
# API_HOST=0.0.0.0
# API_PORT=8000
# LOG_LEVEL=INFO
# COLLECTION_NAME=data_rag_kb

# Features
# ENABLE_CACHING=true
# MCP_ENABLED=true
# DB_QUERY_ENABLED=true

# ================================
# DEPLOYMENT PROFILE (Optional)
# ================================
# Set this to override auto-detection
# DEPLOYMENT_PROFILE=auto  # Options: auto, apple-silicon, nvidia-gpu, cpu-only